<h1 class="mt-3">Редактирование профиля</h1>

<div class="row">
  <div class="col-md-6 mt-3">
    <%= render "user_form" %>

    <h4 class="text-danger mt-5">Удалить мой аккаунт</h4>

    <p><%= button_to "Удалить аккаунт", registration_path(@user),
                     form: { data: { turbo_confirm: t("views.confirm.delete") }},
                     method: :delete, class: "btn btn-danger" %></p>
  </div>
</div>
