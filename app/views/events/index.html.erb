<h1 class="mt-3">События</h1>

<div class="mt-3">
  <%= link_to "Создать событие", new_event_path, class: "btn btn-success"%>
</div>

<h1 class="h2 mt-5">Вы подписаны на:</h1>

<% if @events&.any? %>
  <div class="row">
    <%= render partial: "event", collection: @events %>
  </div>
<% else %>
  <h1 class="text-primary h4 mt-3">Вы не подписаны ни на одно событие</h1>
<% end %>
