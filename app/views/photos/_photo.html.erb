<div class="mx-2 mb-4">
  <%# image_title = "Добавил #{render partial: 'users/user', object: photo.user}" %>

  <%= link_to image_tag(photo.photo.variant(:thumb)), photo.photo.url, data: {
    lightbox: 'event-gallery'
  } %>
</div>
