<div class="column text-center">
  <div class="event-photo mx-2 mb-2">
    <%= link_to image_tag(photo.photo.variant(:thumb)), photo.photo.url, data: {
      lightbox: "event-gallery", title: "Добавил #{photo.user.name}"
    } %>
  </div>
  <div>
    <% if current_user_can_edit?(photo) %>
      <%= button_to bi_icon("trash"),
                       event_photo_path(photo.event, photo),
                       class: "btn btn-link",
                       method: :delete,
                       data: { confirm: t("views.confirm.delete") } %>
    <% end %>
  </div>
</div>

